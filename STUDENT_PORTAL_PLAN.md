# Student Management & Chat Portal - Project Plan

## 1. הרעיון המרכזי
מערכת לניהול תלמידים הכוללת הרשמה לקורסים (בדומה ל"עגלת קניות") ומערכת צ'ט לתקשורת בין התלמידים למורים או בין התלמידים לעצמם. 
האפליקציה שומרת על מבנה הדרישות המקורי (4+ טבלאות קשורות, ניהול Session, ואבטחה), אך נותנת תחושה של מערכת חינוכית/חברתית במקום חנות וירטואלית.

## 2. טכנולוגיות (Skills & Technologies)
* **צד שרת:** Java, Spring Boot, Spring MVC, Spring Data JPA, Spring Security
* **צד לקוח:** HTML5, CSS3, Thymeleaf, וקצת Javascript (לטובת רענון הודעות הצ'ט)
* **מסד נתונים:** MySQL
* **ארכיטקטורה:** MVC, Dependency Injection, Session Management

## 3. הישויות במסד הנתונים (Entities)
כדי להתאים לדרישת ה"הזמנה ועגלת קניות" בצורה טבעית, נשתמש ברישום לקורסים. בנוסף, נוסיף טבלה ייעודית לצ'ט:

1. **User (תלמיד / מורה / אדמין):** 
   * מכיל שם, אימייל, סיסמה, ותפקיד (Role).
   * *קשרים:* One-to-Many עם Registration, One-to-Many עם ChatMessage.
2. **Course (קורס - "המוצר"):** 
   * קורסים הזמינים להרשמה (שם הקורס, תיאור, מכסת תלמידים, מורה אחראי).
   * *קשרים:* One-to-Many עם RegistrationItem.
3. **Registration (בקשת הרשמה - "ההזמנה"):** 
   * טופס הרשמה שמאגד מספר קורסים לאותו סמסטר/שנה.
   * *קשרים:* Many-to-One עם User, One-to-Many עם RegistrationItem.
4. **RegistrationItem (קורס ספציפי בהרשמה - "פריט בהזמנה"):** 
   * קורס ספציפי שנמצא בתוך בקשת ההרשמה.
   * *קשרים:* Many-to-One עם Registration, Many-to-One עם Course.
5. **ChatMessage (ישות חמישית עבור הצ'ט):**
   * תוכן ההודעה, תאריך ושעה.
   * *קשרים:* Many-to-One עם User (השולח), ו-Many-to-One עם User (המקבל) או קשור ל-Course עבור צ'ט קבוצתי.

## 4. איך ה-Session משתלב פה ("העגלה")?
כאשר תלמיד עובר על קטלוג הקורסים ורוצה להירשם אליהם לסמסטר הקרוב, הוא לוחץ על "הוסף להרשמה". הקורסים נשמרים ב-**Session** תחת "עגלת קורסים" (Course Cart). 
רק כאשר התלמיד ניגש לדף הרישום ולוחץ "אשר הרשמה לקורסים", נוצרת רשומת `Registration` קבועה ב-DB (בדיוק כמו Checkout בחנות).

## 5. דפים מרכזיים באפליקציה (לפחות 5 דפים)
1. **Landing / Home Page:** דף נחיתה עם הסבר על הפורטל והתחברות.
2. **Login / Registration Page:** הרשמה למערכת והתחברות עם Spring Security.
3. **Course Catalog (קטלוג קורסים):** רשימת הקורסים הזמינים (כמו חנות מוצרים), עם כפתורי "הוסף למערכת השעות שלי".
4. **Registration Cart (עגלת רישום - Session):** צפייה בכל הקורסים שנבחרו לפני אישור סופי של ההרשמה לסמסטר.
5. **Student Dashboard & Chat:** האזור האישי של התלמיד. מציג את המערכת שאושרה לו, ולידה **ממשק הצ'ט** שמאפשר לשלוח ולקבל הודעות ממורים ומחברים.
6. **Admin/Teacher Dashboard:** ניהול מלאי הקורסים, צפייה בתלמידים הרשומים לכל קורס, וגישה לצ'ט מול התלמידים.

## 6. חלוקת עבודה ל-2 סטודנטים (Division of Tasks)

### סטודנט 1 (דוד)
* **תשתיות ומסד נתונים:** הקמת פרויקט Spring Boot, הגדרת חיבור ל-MySQL (מסד נתונים `ex4`), יצירת ה-Entities (`User`, `Course`, `Registration`, `RegistrationItem`) והקשרים ביניהם.
* **אבטחה והרשאות:** הגדרת Spring Security, יצירת עמודי התחברות והרשמה (Login/Registration).
* **ניהול קורסים:** בניית קטלוג הקורסים (ה"מוצרים") וניהולם.
* **צד לקוח בסיסי:** תבניות Thymeleaf בסיסיות ודף נחיתה.

### סטודנט 2 (שותף)
* **לוגיקה עסקית (העגלה):** מימוש "עגלת קורסים" (Course Cart) מבוססת Session, ולוגיקת אישור הרשמה.
* **מערכת הצ'ט:** יצירת ישות `ChatMessage`, וממשק הצ'ט באזור האישי.
* **אזורים אישיים:** פיתוח אזור אישי לסטודנט ואזור ניהול למורה/אדמין (Dashboards).
* **עיצוב:** עיצוב הדפים (CSS) וקצת JavaScript לרענון ההודעות בצ'ט.

*(הערה: החלוקה לעיל היא לצורך עמידה בדרישות ההגשה הפורמליות. בפועל העבודה תתבצע בהתאם לזמינות חברי הצוות).*
