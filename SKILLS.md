# עקרונות עבודה, דגשים והנחיות לפרויקט (Skills & Guidelines)

קובץ זה נועד לשמש כמצפן לאורך הפיתוח, ומוודא שאנחנו לא סוטים מדרישות פרויקט הגמר (E-Commerce שהומר לפורטל סטודנטים). 

## 1. דרישות חובה והגבלות קריטיות (מה "אסור" לפספס)
* **שם מסד הנתונים:** חובה להגדיר ב-`application.properties` את מסד הנתונים בשם `ex4`.
* **ניהול מצב (Session):** חובה להשתמש ב-HTTP Session עבור "עגלת הקורסים". אסור לשמור את העגלה ישירות במסד הנתונים לפני אישור סופי.
* **מספר ישויות (Entities) וקשרים:** חובה שיהיו לפחות 4 טבלאות המקושרות ביניהן בקשרי One-to-Many ו-Many-to-One. (בפרויקט שלנו יש 5: User, Course, Registration, RegistrationItem, ChatMessage).
* **מספר דפים:** מינימום 5 דפים (אצלנו: התחברות/הרשמה, קטלוג, עגלת רישום, דאשבורד מנהל, דאשבורד סטודנט/צ'ט, דף בית).
* **Javascript:** השימוש ב-JS צריך להיות מוגבל (בעיקר ולידציות). מותר להשתמש במעט JS עבור רענון הצ'ט ב-AJAX, אבל אסור להפוך את הפרויקט ל-SPA (Single Page Application). הרינדור העיקרי חייב להיות בצד שרת עם Thymeleaf.

## 2. כללי פיתוח נכון (Best Practices) ב-Spring Boot
* **הפרדת שכבות (MVC):**
  * `Controllers`: אחראים רק על קבלת הבקשות (HTTP Requests), קריאה ל-Services, והחזרת ה-Views (Thymeleaf templates). אסור לכתוב לוגיקה עסקית ב-Controller.
  * `Services`: כאן תשב כל הלוגיקה העסקית. פנייה למסד הנתונים תתבצע רק דרך שכבה זו.
  * `Repositories`: ממשקי Spring Data JPA בלבד לשליפת נתונים מ-MySQL.
* **הזרקת תלויות (Dependency Injection):** תמיד להשתמש ב-Constructor Injection או ב-`@Autowired` בצורה מסודרת.
* **אבטחה (Spring Security):**
  * סיסמאות **חייבות** להיות מוצפנות במסד הנתונים (למשל בעזרת `BCryptPasswordEncoder`).
  * הגדרת הרשאות חכמה: רק מנהל יכול להוסיף/למחוק קורסים.

## 3. סדר עבודה מומלץ (שלבי פיתוח)
1. **שלב א' - תשתיות:** הקמת הפרויקט, חיבור למסד הנתונים `ex4`, ויצירת ה-Entities ב-JPA.
2. **שלב ב' - אבטחה ומשתמשים:** הגדרת Spring Security ויצירת תהליך הרשמה והתחברות.
3. **שלב ג' - קטלוג מוצרים (קורסים):** הצגת הקורסים (CRUD בסיסי לאדמין ותצוגה לסטודנט).
4. **שלב ד' - לוגיקת העגלה:** מימוש עגלת הרישום באמצעות Session ואישור ההרשמה (שמירה ל-DB).
5. **שלב ה' - צ'ט ודאשבורדים:** מימוש הדאשבורדים האישיים ומערכת הודעות הצ'ט.
6. **שלב ו' - פינישים:** תיקוני עיצוב, וידוא עמידה בכל דרישות המטלה לקראת סרטון ההגשה.

## 4. ממה כדאי להיזהר?
* **"שבירת" טופס הרשמה בגלל CSRF:** כשעובדים עם Spring Security, פוסטים (POST) עלולים להיחסם. יש לוודא הוספת טוקן CSRF בטפסים של Thymeleaf.
* **קריסה בטעינת קשרים ב-JPA:** להיזהר מ-Infinite Recursion ב-JSON/ToString, או משגיאות `LazyInitializationException`. מומלץ להשתמש ב-`FetchType.LAZY` בחוכמה וב-DTOs אם צריך.
* **עיצוב כבד מדי:** אל תשרוף יותר מדי זמן על עיצוב (CSS) בשלבים המוקדמים. הפוקוס הוא על הלוגיקה ב-Java והאבטחה. עיצוב עושים בסוף.
